# 天気ブロック（冒頭・市場データの前）

日報の先頭、対象期間の直後に置く。市場データより前。
解説や「なぜ重要か」は付けない。

## 取得元

| 用途 | URL |
|---|---|
| 予報 | https://www.jma.go.jp/bosai/forecast/data/forecast/130000.json |
| 警報 | https://www.jma.go.jp/bosai/warning/data/warning/130000.json |

いずれも素のJSONで返る（JS描画ではない）。毎朝7時の実行時に取得し、
data.json の `weather` キーに `{"forecast": ..., "warning": ...}` の形で
そのまま格納する。加工は `weather_block.py` の
`render_weather(weather, today, now_hour)` が行い、`(html, notes)` を返す。

## 抜き出す場所

| 項目 | パス |
|---|---|
| 天気文 | `[0].timeSeries[0].areas[name="東京地方"].weathers` |
| 天気コード | `[0].timeSeries[0].areas[name="東京地方"].weatherCodes` |
| 最低/最高 | `[0].timeSeries[2].areas[name="東京"(44132)].temps`（00:00=最低、09:00=最高） |
| 降水確率 | `[0].timeSeries[1].areas[name="東京地方"].pops`（6時間区切り、残り時間帯のみ） |
| 平年値 | `[1].tempAverage.areas[name="東京"]` |
| 発表時刻 | `[0].reportDatetime` を「M/D H時発表」で明記 |

配列の位置は発表回（5時／11時／17時）で変わるため、添字を決め打ちしない。
`timeDefines` の日付で当日ぶんを探す。伊豆諸島・小笠原の値と取り違えない。

## 日付ガード（必須）

気象庁JSONはキャッシュで古い内容が返ることがある。2026-09-10の検証では
警報JSONが `reportDatetime` 2026-05-28 を返した。

- 予報：`reportDatetime` が当日なら通常表示。前日なら発表時刻を明記して使う。
  それより古ければ天気ブロックごと省略する。
- 警報：当日または前日でなければ警報バンドを出さない。
  古いだけで「警報なし」と書かない。未取得として扱う。
- 省略・代替が起きた場合は日報末尾の注記に理由を書く（`notes` がそのまま使える）。

## 警報の出し分け

- 見る場所は `areaTypes[0].areas[code="130010"]`（東京地方）。
- `status` が「発表警報・注意報はなし」なら警報バンドを出さない。
- `status` が「解除」の項目は除く。
- 注意報だけの日は出さない。警報・特別警報・危険警報のみ表示する。

コード対応（第三者記事2件で一致、気象庁の公式表は未確認）

- 警報：02暴風雪 03大雨 04洪水 05暴風 06大雪 07波浪 08高潮 09土砂災害
- 特別警報：32暴風雪 33大雨 35暴風 36大雪 37波浪 38高潮 39土砂災害
- 危険警報：43大雨 48高潮 49土砂災害
- 注意報（表示しない）：10, 12〜26, 29

## 表示仕様

カード（`--surface-2` / 0.5px `--border` / 12px radius / padding 1rem 1.25rem）。

- 上段：地域名（東京地方）と「気象庁 M/D H時発表」
- 中段：天気アイコン＋天気文＋最高/最低気温。平年値との差が±3℃以上なら
  「平年より高め／低め」を副文に出す
- 下段：降水確率のセル（当日の残り時間帯のみ）
- 警報時：カード最上部に `--bg-danger` のバンドを追加

アイコンと色

| 天気 | 判定 | アイコン | 色 |
|---|---|---|---|
| 晴れ | コード1xx | ti-sun | --text-warning |
| くもり | コード2xx | ti-cloud | --text-secondary |
| 雨 | コード3xx | ti-cloud-rain | --text-accent |
| 雪 | コード4xx | ti-cloud-snow | --text-accent |
| 雷 | 天気文に「雷」 | ti-cloud-storm | --text-pro |
| 霧 | 天気文に「霧」 | ti-cloud-fog | --text-secondary |

「雷」「霧」はコードより優先する。`ti-cloud-sun` のような複合名は
Tablerでの有無が未確認のため使わない。

降水確率セルの濃さ

`background: color-mix(in srgb, var(--fill-accent) N%, transparent)`

| 確率 | N |
|---|---|
| 0-19% | 塗らない（--surface-1） |
| 20-39% | 10 |
| 40-59% | 24 |
| 60-79% | 38 |
| 80-100% | 48 |

文字は `--text-primary` のまま。`color-mix` を使うのでダークモードは自動追従する。

## 検証済みのケース

2026-09-10 に実データで確認。

| ケース | 結果 |
|---|---|
| 予報9/9 17時発表＋警報5/28（キャッシュ） | 天気は発表時刻明記で表示、警報は省略して注記 |
| 注意報のみ（雷・強風） | 警報バンドなし |
| 大雨警報＋洪水警報＋雷注意報＋波浪警報解除 | バンドに「大雨警報・洪水警報」 |
| 予報も古い（5/28） | 天気ブロックごと省略して注記 |

いずれも `render_news.py` + `news-daily-template.html` の通しで出力を確認済み。

## 未確認

- 朝7時の時点で当日5時発表が反映されるか。翌日の実行で実測する。
- 警報バンドに市区町村（例：23区東部）まで出すか。出す場合は
  `areaTypes[1]` の市区町村コードが必要（江東区=1310800は推定、未確認）。
